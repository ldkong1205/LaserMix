# TRAINING CONFIG
EXP_NAME: 'range.sup_only'
SUFFIX: ''

DATA:
    DATASET: 'nuscenes'  # choices: ['nuscenes', 'semantickitti', 'scribblekitti']
    SPLIT: '1pct'  # choices: ['1pct', '10pct', '20pct', '50pct']

MODEL:
    MODALITY: 'range'  # choices: ['range', 'voxel']
    RESOLUTION:
        NUSCENES: [32, 1920]
        SEMANTICKITTI: [64, 2048]
        SCRIBBLEKITTI: [64, 2048]

TRAIN:
    NUM_EPOCHS: 50
    BATCH_SIZE: 1
    NUM_WORKERS: 0

VALID:
    BATCH_SIZE: 1
    NUM_WORKERS: 0
    EPOCH_START_VAL: 0

OPTIM:
    LOSS: 'dice'  # choices: ['wce', 'dice']
    IF_LS_LOSS: True
    IF_BD_LOSS: True
    TOP_K_PERCENT_PIXELS: 1.0
    OPTIMIZER: 'adamw'  # choices: ['adamw', 'sgd']
    LEARNING_RATE: 0.0025
    SCHEDULER: 'onecycle'  # choices: ['onecycle', 'step']

GENERAL:
    SEED: 1205


